# Daymark currently has no required runtime environment variables.
# Variables prefixed with VITE_ are exposed to every browser visitor.
# Do not put passwords, API keys, or tokens in this file.
VITE_APP_NAME=Daymark
