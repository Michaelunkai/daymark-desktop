{
  "name": "Daymark Task Assistant",
  "version": 3,
  "integration": "openapi-http",
  "manifest": "/daymark-agent.json",
  "openapi": "/api/agent/v1/openapi.json",
  "authentication": {
    "scheme": "bearer",
    "provisioning": "Generate a task assistant key in Daymark Settings. The secret is shown once and can be revoked from Settings."
  },
  "operations": [
    "listProjects",
    "listTasks",
    "createTask",
    "completeTask"
  ],
  "excludedData": [
    "notes",
    "diary",
    "backups",
    "syncState",
    "pairingCodes"
  ],
  "excludedActions": [
    "task.delete",
    "project.create",
    "project.update",
    "project.delete",
    "backup.import",
    "backup.export",
    "sync.manage"
  ],
  "idempotency": "All write operations require an Idempotency-Key header. Retry a conflict with the same key.",
  "legacyBrowserBridge": "DaymarkAI remains an in-page compatibility adapter only. It is not an authenticated remote API."
}
